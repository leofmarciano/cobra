# diagnostics

This directory implements diagnostic reporting, error messages, warnings, and source location tracking for the Python frontend. Diagnostics are designed to be actionable and integrate with both the CLI and IDE tooling.
