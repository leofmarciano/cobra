# _native

This directory houses native extension bindings and generated wrappers that expose the C++ Cobra compiler and runtime to Python. It is named with a leading underscore to indicate a private implementation detail of the Python package.
