# adapters

This directory contains adapters that integrate Cobra with popular Python frameworks and libraries such as PyTorch, JAX, TensorFlow, and DataFrame packages. Adapters normalize framework-specific constructs into forms the Cobra compiler can process.
