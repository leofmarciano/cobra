# backends

This directory contains Python-level backend bindings and drivers that connect the Cobra compiler to target runtimes and code generators. Backends here coordinate with native libraries to emit and execute optimized kernels on CPU, GPU, and distributed targets.
