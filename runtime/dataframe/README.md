# dataframe

This directory implements the DataFrame runtime, including query execution engines, columnar memory formats, and operators for filtering, aggregation, and joins. It supports both local and distributed DataFrame execution plans.
