# cuda

This directory implements the NVIDIA CUDA backend of the Cobra runtime, including kernel launches, stream management, device memory allocation, and cuBLAS or cuDNN integrations. It targets single-node GPU acceleration and coordinates with the core runtime for cross-device execution.
