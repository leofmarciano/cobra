# distributed

This directory implements the distributed runtime for Cobra, providing collective communication, remote task scheduling, and partitioning primitives. It enables scaling compiled programs across multiple nodes while integrating with the core runtime scheduler.
