# runtime

This directory contains the heterogeneous parallel runtime implementation that executes compiled Cobra programs. It provides cross-device scheduling, memory management, and communication primitives for CPU, GPU, and distributed execution.
