# core

This directory contains core runtime abstractions shared by all backend implementations, including task graphs, event systems, memory pools, and device contexts. Core components define the portable interfaces used by higher-level runtime modules.
