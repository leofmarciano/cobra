# cpu

This directory implements the CPU backend of the Cobra runtime, including thread pools, SIMD-optimized kernels, and cache-aware schedulers. It targets high single-node CPU performance for both scalar and vectorized workloads.
