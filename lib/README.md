# lib

This directory contains the core C++ implementation libraries that make up the Cobra compiler and runtime interfaces. Each subdirectory groups related functionality, such as analysis passes, MLIR dialects, conversions, optimizations, and support utilities.
