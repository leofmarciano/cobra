# CobraEffect

This directory defines the CobraEffect MLIR dialect, which represents effectful operations and effect summaries explicitly in the IR. It supports optimizations that must reason about side effects while preserving observable behavior.
