# Dialect

This directory contains MLIR dialect definitions, operations, types, and passes used by the Cobra compiler. Each dialect models a distinct abstraction layer, from high-level program and tensor representations down to runtime and placement concerns.
