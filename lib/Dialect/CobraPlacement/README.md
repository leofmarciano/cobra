# CobraPlacement

This directory defines the CobraPlacement MLIR dialect, which captures device and placement annotations for heterogeneous execution. Placement operations guide the planner and runtime when distributing work across CPU, GPU, and distributed resources.
