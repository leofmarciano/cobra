# CobraDataFrame

This directory defines the CobraDataFrame MLIR dialect, which represents tabular data operations such as projection, filtering, grouping, and joins. The dialect enables DataFrame-specific optimizations and lowering to query execution plans or vectorized kernels.
