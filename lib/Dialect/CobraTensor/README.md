# CobraTensor

This directory defines the CobraTensor MLIR dialect, which models dense and sparse tensor operations with shape, dtype, and layout information. It bridges high-level program constructs with efficient backend kernels and supports tensor-specific optimizations.
