# AliasAnalysis

This directory implements alias and pointer analysis for Cobra programs. The results are used by optimization passes, the planner, and code generation to determine when memory objects may overlap and when it is safe to reorder or parallelize operations.
