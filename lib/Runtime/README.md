# Runtime

This directory contains compiler-side runtime interfaces and support libraries that link compiled Cobra programs to the heterogeneous parallel runtime. It defines ABI contracts, dispatch tables, and utilities used during code generation and execution.
