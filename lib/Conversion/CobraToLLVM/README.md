# CobraToLLVM

This directory implements the lowering of Cobra IR to LLVM IR for CPU code generation. It translates Cobra runtime and compute operations into LLVM instructions, enabling compilation to native object code via existing LLVM toolchains.
