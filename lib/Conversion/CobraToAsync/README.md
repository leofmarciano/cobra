# CobraToAsync

This directory implements the lowering of Cobra IR to asynchronous execution primitives. It introduces async operations, futures, and scheduling boundaries that enable overlapping computation, memory movement, and I/O.
