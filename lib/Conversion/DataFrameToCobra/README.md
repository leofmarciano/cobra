# DataFrameToCobra

This directory implements the conversion from DataFrame library APIs, such as pandas and Polars, into the CobraDataFrame dialect. It captures tabular operations and enables DataFrame-aware optimization and backend lowering.
