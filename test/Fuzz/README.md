# Fuzz

This directory contains fuzz tests and input generators that stress Cobra's parser, dialect validation, and optimization pipelines with randomized programs. Fuzzing complements unit tests by exploring edge cases that are hard to anticipate.
