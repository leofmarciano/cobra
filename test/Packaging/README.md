# Packaging

This directory contains tests that validate the packaging, installation, and distribution of the Cobra compiler and runtime. Tests cover wheel builds, container images, dependency constraints, and import behavior in fresh environments.
