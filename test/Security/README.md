# Security

This directory contains security-focused tests, including sandboxing, untrusted input validation, and denial-of-service checks. Tests ensure that Cobra tools and runtime handle malicious or malformed inputs without compromising the host system.
