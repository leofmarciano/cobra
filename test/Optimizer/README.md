# Optimizer

This directory contains tests for Cobra optimization passes, validating transformations such as fusion, layout selection, and constant propagation. Tests check both that optimizations are applied when expected and that they never alter observable program behavior.
