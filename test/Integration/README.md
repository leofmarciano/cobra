# Integration

This directory contains end-to-end integration tests that exercise the full Cobra pipeline from Python capture through compilation to execution. Tests validate real-world workloads and catch issues that only appear when components interact.
