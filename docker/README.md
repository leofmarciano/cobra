# docker

This directory contains Dockerfiles, compose definitions, and container configuration for building and running Cobra in reproducible environments. Images defined here are used for CI, development, and deployment on Linux x86_64 with NVIDIA CUDA support.
