# c_api

This directory contains the public C API headers used to integrate Cobra with foreign-function interfaces and language bindings. The C API provides a stable, ABI-compatible surface for embedding the compiler and runtime in other projects.
