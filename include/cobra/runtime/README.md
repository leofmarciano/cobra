# runtime

This directory contains public headers for Cobra's heterogeneous parallel runtime. These headers define the interfaces used by compiled programs and external tools to schedule work across CPU, GPU, and distributed backends.
