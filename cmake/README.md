# cmake

This directory contains CMake modules, build configuration helpers, and find scripts used by the Cobra project. Centralizing build logic here keeps the top-level CMakeLists.txt focused on project structure while enabling reusable, versioned configuration for dependencies, compiler flags, and packaging rules.
