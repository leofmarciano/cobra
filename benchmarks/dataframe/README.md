# dataframe

This directory contains DataFrame benchmark workloads, covering tabular operations such as joins, aggregations, filtering, and groupings. These benchmarks evaluate Cobra's DataFrame runtime and compiler optimizations against pandas, Polars, and other baselines.
