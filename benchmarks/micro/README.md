# micro

This directory contains microbenchmarks that isolate individual Cobra compiler and runtime operations, such as dispatch overhead, memory copies, and kernel launch latency. Microbenchmarks are useful for spotting regressions in specific components.
